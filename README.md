# Ansible role mkcert.

Ansible role to create a Test Lab Certificate Authority and server certificate signed by CA using OpenSSL. Includes Vagrant (VirtualBox provider) example which starts RedHat (using AlmaLinux) Family machines and Debian Family machines.

Includes an Apache example role to provide an example of how to use the server certificate with orher Ansible roles.
